<%= form_with(model: created_day, local: true) do |form| %>
  <% if created_day.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(created_day.errors.count, "error") %> prohibited this created_day from being saved:</h2>

      <ul>
        <% created_day.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :day %>
    <%= form.date_select :day %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
